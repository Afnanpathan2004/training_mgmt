# In all relevant places, replace 'Calendar' with 'Calendar Program' and 'Non Calendar' with 'Non Calendar Program'.
# For example:
# Program.objects.filter(program_type='Calendar') -> Program.objects.filter(program_type='Calendar Program')
# Training.objects.filter(program_type='Calendar') -> Training.objects.filter(program_type='Calendar Program')
# And so on for all logic, filters, and assignments.
# Also update any default values to 'Calendar Program'.
# ... existing code ... 